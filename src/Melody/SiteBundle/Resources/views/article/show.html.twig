{% extends 'MelodySiteBundle::base.html.twig' %}

{% set namespace = _self.getTemplateName()|split(':') %}
{% block file %}{{ namespace[1] }}/{{ namespace[2] }}{% endblock %}

{% block body %}
    <a href="{{ path('article_index') }}"><i class="fa fa-arrow-circle-o-left" aria-hidden="true"></i> Back to the list</a>
    <h1>{{ article.title }}</h1>
    <div>{{ article.text }}</div>
{% endblock %}
